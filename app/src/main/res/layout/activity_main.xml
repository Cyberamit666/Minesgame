<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0F0F0F"
    android:padding="16dp">

    <TextView android:id="@+id/tv_balance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="#00FF88" />

    <LinearLayout android:id="@+id/menu_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button android:id="@+id/btn_single" android:text="Single Player (Bet)" />
        <Button android:id="@+id/btn_2p" android:text="2P Offline Hotseat" />
    </LinearLayout>

    <LinearLayout android:id="@+id/game_panel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone">

        <GridView android:id="@+id/grid_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="8dp" />

        <TextView android:id="@+id/tv_multiplier" android:text="Multiplier: x1.00" android:textSize="18sp"/>
        <TextView android:id="@+id/tv_winnings" android:text="Winnings: 0" android:textSize="18sp"/>
        <Button android:id="@+id/btn_cashout" android:text="CASH OUT" />
        <Button android:id="@+id/btn_newgame" android:text="New Game / Menu" />
    </LinearLayout>
</LinearLayout>